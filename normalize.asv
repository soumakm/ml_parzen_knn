% This function will normalize the input matrix x
% input :
%       x = input matrix
%       y = normalized output


%function y = normalize(x)
close all;
clear;

x = magic(5)
rows = size(x,1);
cols = size(x,2);

m = mean(x);
for i=1:rows
    x1(i,:) = x(i,:) - m;
end
x1
v = var(x1);


